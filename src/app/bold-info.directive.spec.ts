import { BoldInfoDirective } from './bold-info.directive';

describe('BoldInfoDirective', () => {
  it('should create an instance', () => {
   // const directive = new BoldInfoDirective();
    //expect(directive).toBeTruthy();
  });
});
